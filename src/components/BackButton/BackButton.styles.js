import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  backButton: {
    marginLeft: 20
  },
  backButtonText: {
    paddingLeft: 10,
    fontSize: 16,
    color: '#fff'
  }
});
